{
  "template_id": "lead-acquisition",
  "version": "1.0.0",
  "name": "线索采集与触达",
  "description": "抓取评论 → LLM 评估 → 按策略评论/关注/私信。",
  "executor_id": "lead_acquisition",
  "platforms": ["douyin", "xiaohongshu"],
  "phases": [
    { "id": "crawl", "capability": "pipeline_keyword_comments", "retry": 2, "depends_on": [] },
    { "id": "normalize", "capability": "internal.normalize_leads", "retry": 0, "depends_on": ["crawl"] },
    { "id": "evaluate", "capability": "internal.evaluate_leads", "retry": 1, "depends_on": ["normalize"] },
    { "id": "outreach", "capability": "skill_flow_outreach", "retry": 1, "depends_on": ["evaluate"] },
    { "id": "finalize", "capability": "internal.summarize", "retry": 0, "depends_on": ["outreach"] }
  ],
  "default_spec": {
    "task_name": "",
    "keyword": "",
    "platform": "douyin",
    "account_id": "default",
    "region": null,
    "crawl": {
      "video_publish_days": null,
      "comment_days": 3,
      "target_leads": 100,
      "video_limit_per_batch": 20,
      "max_batches": 50,
      "force_refresh": false
    },
    "evaluation": {
      "precise_threshold": 0.60,
      "outreach_threshold": 0.55
    },
    "action_policy": {
      "comment_ratio": 50,
      "dm_ratio": 30,
      "follow_ratio": 20,
      "interval_min_sec": 10,
      "interval_max_sec": 30
    },
    "daily_limits": {
      "max_comment_replies": 30,
      "max_follows": 30,
      "max_dms": 30
    },
    "provider": "deepseek",
    "timeout_seconds": 1200
  }
}
